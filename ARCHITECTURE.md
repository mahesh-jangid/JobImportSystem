# System Design & Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Admin Dashboard (React + Next.js)                        │   │
│  │  - Import History View                                    │   │
│  │  - Statistics & Metrics                                   │   │
│  │  - Job Listings                                           │   │
│  │  - Real-time Updates                                      │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↑
                              │ HTTP/REST
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        API LAYER                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Next.js API Routes (Node.js)                             │   │
│  │  GET /api/jobs                (List jobs)                │   │
│  │  GET /api/jobs/import-history (Import history)           │   │
│  │  GET /api/jobs/stats          (Statistics)               │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              ↑↓
            ┌─────────────────────────────────┐
            │      SERVICE LAYER              │
            ├─────────────────────────────────┤
            │ JobImportService:               │
            │ - Import to MongoDB             │
            │ - Log statistics                │
            │ - Handle duplicates             │
            └─────────────────────────────────┘
```

## 📊 Data Flow Diagram

```
EXTERNAL JOB APIs
  │ Jobicy (9 feeds)
  │ Higher Ed Jobs
  │ Custom feeds
  └──────────────────────┐
                         │
                    ┌────▼──────────┐
                    │   CRON JOB    │
                    │  (Every 1hr)  │
                    └────┬──────────┘
                         │
                    ┌────▼──────────────────┐
                    │  JOB FETCHER SERVICE  │
                    │                       │
                    │ 1. Fetch XML from API │
                    │ 2. Parse XML → JSON   │
                    │ 3. Extract metadata   │
                    │ 4. Queue jobs         │
                    └────┬──────────────────┘
                         │
                    ┌────▼──────────────┐
                    │   REDIS QUEUE     │
                    │   (BullMQ)        │
                    │                   │
                    │ - Stores jobs     │
                    │ - Retry logic     │
                    │ - Concurrency: 5  │
                    └────┬──────────────┘
                         │
          ┌──────────────┼──────────────┐
          │              │              │
          ▼              ▼              ▼
      ┌────────┐    ┌────────┐    ┌────────┐
      │ Worker │    │ Worker │    │ Worker │
      │   #1   │    │   #2   │    │   #3   │
      └────┬───┘    └────┬───┘    └────┬───┘
           │             │             │
           └─────────────┼─────────────┘
                         │
                    ┌────▼──────────────┐
                    │  JOB IMPORT SERV. │
                    │                   │
                    │ 1. Check duplicate│
                    │ 2. Insert/Update  │
                    │ 3. Log results    │
                    └────┬──────────────┘
                         │
           ┌─────────────┴──────────────┐
           │                            │
           ▼                            ▼
      ┌────────────┐            ┌────────────┐
      │  MongoDB   │            │  MongoDB   │
      │  - Jobs    │            │  - Logs    │
      │  Collection│            │  Collection│
      └────────────┘            └────────────┘
           │                            │
           └─────────────┬──────────────┘
                         │
                    ┌────▼────────────┐
                    │ Admin Dashboard │
                    │  (View History) │
                    └─────────────────┘
```

## 🔄 Import Process Sequence Diagram

```
Time │ Cron    │ Fetcher     │ Queue        │ Worker       │ MongoDB
─────┼─────────┼─────────────┼──────────────┼──────────────┼─────────
     │ ▼       │             │              │              │
     │ Trigger │             │              │              │
     │         ├─────────────▶ For each URL │              │
     │         │ Add to Queue│              │              │
     │         │             │ ▼            │              │
     │         │             │ Fetch XML    │              │
     │         │             ├──────────────▶ Pick up job  │
     │         │             │              │              │
     │         │             │              │ Parse XML    │
     │         │             │              │ Convert JSON │
     │         │             │              │ Extract data │
     │         │             │              │              │
     │         │             │              │ For each job:│
     │         │             │              │ - Check dup  │
     │         │             │              │ - Insert/Upd │
     │         │             │              │              │
     │         │             │              │ ├────────────▶ Create/Update
     │         │             │              │              │
     │         │             │              │ Log results  │
     │         │             │              │ ├────────────▶ Insert Log
     │         │             │              │              │
     │         │             │              │ Done!        │
     │         │             │              │              │
     │ ─────────────────────────────────────── 1 hour ────────
```

## 📦 Component Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                      Frontend                                     │
│                   (Next.js + React)                              │
│                  import-history/page.tsx                         │
└───────────────────┬───────────────────────────────────────────────┘
                    │
                    │ HTTP Request
                    ▼
┌──────────────────────────────────────────────────────────────────┐
│                    API Routes                                     │
│  ┌───────────────────┐  ┌───────────────────┐                    │
│  │ api/jobs          │  │ api/jobs/stats    │                    │
│  ├───────────────────┤  ├───────────────────┤                    │
│  │ - Fetch jobs      │  │ - Aggregation     │                    │
│  │ - Filter          │  │ - Per source      │                    │
│  │ - Paginate        │  │ - Statistics      │                    │
│  └────────┬──────────┘  └────────┬──────────┘                    │
│           │                      │                               │
│           └──────────────┬───────┘                               │
└────────────────────────────┼──────────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
    ┌─────────┐         ┌─────────┐        ┌──────────┐
    │ Service │         │ Service │        │ Service  │
    │ JobImp. │         │ JobFetch│        │ QueueMgr │
    └────┬────┘         └────┬────┘        └────┬─────┘
         │                   │                   │
         │                   │  ┌────────────────┘
         │                   ▼  ▼
         │              ┌─────────────┐
         │              │   Worker    │
         │              │ (jobWorker) │
         │              └──────┬──────┘
         │                     │
         │        ┌────────────┼────────────┐
         │        │            │            │
         ▼        ▼            ▼            ▼
    ┌─────────────────┐  ┌─────────┐  ┌──────────┐
    │    MongoDB      │  │   Cron  │  │  Redis   │
    │  - jobs         │  │ Scheduler │ │ Queue    │
    │  - importlogs   │  └─────────┘  └──────────┘
    └─────────────────┘
```

## 🔌 Service Dependencies

```
Frontend Layer
    ↓
API Routes Layer
    ├─── JobImportService ─────→ MongoDB (importlogs)
    │
    ├─── JobFetcherService ────→ External APIs
    │                     ↓
    │                  QueueManager
    │                     ↓
    │                  Redis Queue
    │                     ↓
    │                  Worker
    │                     ↓
    │                MongoDB (jobs)
    │
    └─── (Cron Scheduler) ──────→ QueueManager
                            ↓
                          Worker
```

## 📊 Data Schema Relationships

```
┌──────────────────────────────────────────────────────────────┐
│                    MongoDB Collections                        │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─ Jobs Collection ──────────────────────────────────────┐ │
│  │                                                        │ │
│  │ ┌─ Indexes:                                          │ │
│  │ │  - title (text search)                             │ │
│  │ │  - company (search filter)                         │ │
│  │ │  - source (job source)                             │ │
│  │ │  - category (job type)                             │ │
│  │ │  - link (unique identifier)                        │ │
│  │ │  - externalId (external API ID)                    │ │
│  │ │                                                     │ │
│  │ ├─ Fields:                                           │ │
│  │ │  - title, description, company                    │ │
│  │ │  - location, jobType, category                    │ │
│  │ │  - salary, link, source                           │ │
│  │ │  - postedDate, isActive                           │ │
│  │ │  - createdAt, updatedAt                           │ │
│  │ │                                                     │ │
│  │ └─ Usage: Job listings, search, filtering           │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
│  ┌─ ImportLogs Collection ────────────────────────────────┐ │
│  │                                                        │ │
│  │ ┌─ Indexes:                                          │ │
│  │ │  - source (statistics)                             │ │
│  │ │  - timestamp (chronological)                       │ │
│  │ │  - status (success/failed)                         │ │
│  │ │                                                     │ │
│  │ ├─ Fields:                                           │ │
│  │ │  - url, source                                     │ │
│  │ │  - totalFetched, totalImported                     │ │
│  │ │  - newJobs, updatedJobs, failedJobs               │ │
│  │ │  - failedJobsDetails[]                             │ │
│  │ │  - duration, status, errorMessage                 │ │
│  │ │  - timestamp, createdAt, updatedAt                │ │
│  │ │                                                     │ │
│  │ └─ Usage: History tracking, statistics              │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Error Handling Flow

```
┌─────────────────────────────┐
│ Job Added to Queue          │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ Worker Picks Up Job         │
└────────────┬────────────────┘
             │
             ├─── Success ─┐
             │             │
             │ Error       ▼
             │        ┌────────────────────────┐
             │        │ Log Error in ImportLog │
             │        └────────────────────────┘
             │
             └─── Retry? ─── YES ────┐
                                     │
                    ┌────────────────┤
                    │                │
                    ▼                │
              ┌──────────┐           │
              │ Attempt  │           │
              │ < 3?     │           │
              └─────┬────┘           │
                    │ YES            │
                    ├── Backoff ─────┘
                    │
                    │ NO
                    ▼
              ┌──────────────┐
              │ Failed - Move │
              │ to DLQ       │
              └──────────────┘
```

## 📈 Scalability Plan

```
Current Architecture
├─ Single Worker: Concurrency 5
├─ Single Redis Instance
├─ Single MongoDB Instance
└─ Single Cron Job

Scale to Multiple Workers
├─ Deploy multiple jobWorker instances
│  - Each with concurrency: 5
│  - Total throughput: 5 x N
│  - Shared Redis Queue
│  - Shared MongoDB
│
├─ Add Redis Cluster (for high availability)
│  - Master-Slave replication
│  - Sentinel for failover
│  - Data persistence
│
├─ Add MongoDB Sharding
│  - Partition by source
│  - Reduce query time
│  - Increase storage
│
└─ Separate Cron Services
   - Multiple cron instances
   - Distributed locking
   - Avoid duplicate runs
```

---

**This architecture ensures:**
- ✅ **Scalability** - Add workers without code changes
- ✅ **Reliability** - Queue-based with retries
- ✅ **Maintainability** - Clear separation of concerns
- ✅ **Monitoring** - Complete tracking and logging
- ✅ **Performance** - Optimized queries and indexing
